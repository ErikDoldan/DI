<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/authGateFragment">

    <fragment
        android:id="@+id/loginFragment"
        android:name="com.example.aulamas.LoginFragment"
        android:label="Login"
        tools:layout="@layout/fragment_login">

        <action
            android:id="@+id/action_login_to_home"
            app:destination="@id/homeFragment"
            app:popUpTo="@id/loginFragment"
            app:popUpToInclusive="true"/>

        <action
            android:id="@+id/action_login_to_register"
            app:destination="@id/registerFragment" />
    </fragment>

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.aulamas.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">
    <action
        android:id="@+id/action_home_to_gate"
        app:destination="@id/authGateFragment"
        app:popUpTo="@id/nav_graph"
        app:popUpToInclusive="true"/>
    </fragment>

    <fragment
        android:id="@+id/registerFragment"
        android:name="com.example.aulamas.RegisterFragment"
        android:label="Registro">
        <action
            android:id="@+id/action_register_to_home"
            app:destination="@id/homeFragment"
            app:popUpTo="@id/registerFragment"
            app:popUpToInclusive="true"/>
    </fragment>

    <fragment
        android:id="@+id/authGateFragment"
        android:name="com.example.aulamas.AuthGateFragment"
        android:label="AuthGate">

        <action
            android:id="@+id/action_gate_to_login"
            app:destination="@id/loginFragment"
            app:popUpTo="@id/authGateFragment"
            app:popUpToInclusive="true"/>

        <action
            android:id="@+id/action_gate_to_home"
            app:destination="@id/homeFragment"
            app:popUpTo="@id/authGateFragment"
            app:popUpToInclusive="true"/>
    </fragment>
</navigation>